def make_negative(number):
    number = user
    if user < 0 :
        print(user)
        return user

    elif user >= 0:
        a = user * -1
        print(a)
        return a

user = int(input("Введите число:"))
make_negative(user)